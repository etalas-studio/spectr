# Agents

## Branch & PR rules

PRs go to **`staging`**, not `master`.

`master` is production. All work merges to `staging` first, gets verified there, then staging is promoted to master for release.
